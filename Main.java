public class Main {
  public static void main(String[] args) {

  VirtualPet pet = new VirtualPet();

  // pet.startGame() to start, pet.skipScene(n) to skip to scene n
  // prepare for school with pet.eat() and pet.sleep()

  pet.eat();
  pet.startGame();

  }
}